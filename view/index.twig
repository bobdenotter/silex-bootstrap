{% extends '_base.twig' %}

{% block title %}
    {{ title|default("No title") }} | {{ config.title }}.
{% endblock %}

{% block content %}
    {{ content|default("No content") }}

    {{ dump(config) }}

{% endblock %}
